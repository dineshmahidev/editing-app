# Environment configuration for Vixel Edit
# Copy to .env and fill in real values. NEVER commit real keys.

# GIPHY API key (SDK key) - https://developers.giphy.com/
EXPO_PUBLIC_GIPHY_API_KEY=

# Backend base URL for the optional cloud sync/auth layer
EXPO_PUBLIC_API_URL=http://localhost:8000
